Archivos obsoletos movidos desde el proyecto principal.

- `intro.css` : estilos legacy no referenciados.
- `products.js` : placeholder de datos, actualmente no usado.

Se mantienen aquí por si es necesario recuperar fragmentos. Para eliminar definitivamente, borrar esta carpeta.
